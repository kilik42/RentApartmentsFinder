'use strict';

describe('rentfinds01.rentals module', function() {

  beforeEach(module('rentfinds01.rentals'));

  describe('rentals controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var RentalsCtrl = $controller('RentalsCtrl');
      expect(RentalsCtrl).toBeDefined();
    }));

  });
});
